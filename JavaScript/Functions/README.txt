topics covered:  (functions)

# map :
---- if we need to update property use map and it will return a new array.
---- map return undefined for current item when no 'return' execute for that item
---- if curley braces used then required return key word
---- .map(()=>{
		return ------
	})
---- .map(()=>(
		 ------
	))

- forEach (not learned yet)
- reduce
- find
- filter
- some
- every
- includes
- findindex
- sort (It is not present in the code)
- split
- slice 
- push 
- pop
- toString (convert anything into string specially number, date)
- toFixed
- startsWith (It is not present in the code)
- parseInt
- join
